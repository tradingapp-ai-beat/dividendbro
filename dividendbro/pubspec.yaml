name: trading_advice_app_v2
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  image_picker: ^0.8.4+4
  provider: ^6.0.0
  #amplify_flutter: ^0.2.9
  #amplify_auth_cognito: ^0.2.9
  cupertino_icons: ^1.0.6
  hive: ^2.0.5
  hive_flutter: ^1.1.0
  path_provider: ^2.0.5
  flutter_lints: ^3.0.0
  build_runner: ^2.1.7
  hive_generator: ^1.1.0
  firebase_core: ^2.10.0
  cloud_firestore: ^4.5.0
  firebase_auth: ^4.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
